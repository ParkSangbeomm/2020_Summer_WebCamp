JS Window : window.document.getElementById("header"); == document.getElementById("header");
            윈도우 내부의 사이즈를 재는 방법 - window.innerHeight == document.body.clientHeight
                                              window.innerWidth == document.body.clientWidth
                 
JS Screen : screen.width screen.height screen.availWidth screen.availHeight screen.colorDepth screen.pixelDepth
            쓸 때 body에다가 <div id="demo"></div> <script>document.getElementById("demo").innerHTML = "Screen width" + screen.width; 이런식으로 사용함
            
JS Loacation :  window.location.href returns the href (URL) of the current page
                window.location.hostname returns the domain name of the web host
                window.location.pathname returns the path and filename of the current page
                window.location.protocol returns the web protocol used (http: or https:)
                window.location.assign() loads a new document
                
JS History : back and forwoard 두개가 있다. window.history.back() 하면 지난 페이지로 돌아가고 window.history.forward()하면 앞으로간다.

JS Navigator : 쿠키가 있는지. 사용하는 Application Name 이 뭔지, 자바는 사용가능한지 그런거를 그냥 물으면 true false 혹은 단답형으로 말해준다.

JS popup alert : alert() 이거는 그냥 뜨는거, confirm("text") text안에 글씨가 나오면서 확인과 취소를 누를 수 있는 좋은 기능 반환값은 true false
                 prompt("a", "b") 첫번째 파라미터는 알림뜰때 같이 뜨는 거고 b는 그 넣는 값 디폴트이다. 
                 
JS Timing : setTimeout(a, 2000) 이런 식으로 몇초뒤에 a를 실행할건지 가능하게 한다. 
            clearTimeout을 이용하면 멈추게할 수 있다.
